import { VNode } from 'maquette';
import * as Values from '../definitions/values';
declare function printPreview(cellId: number, triggerSelect: (number) => void, selectedTable: number, cellValues: Values.ExportsValue): VNode;
export { printPreview };
