/** @hidden */
/** This comment is needed so that TypeDoc parses the above one correctly */
import { VNode } from 'maquette';
import * as Values from '../definitions/values';
declare function printPreview(cellId: number, triggerSelect: (number: number) => void, selectedTable: number, cellValues: Values.ExportsValue): VNode;
export { printPreview };
